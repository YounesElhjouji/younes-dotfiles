# auto-shutdown-if-no-ssh.timer
#
# (Cheatsheet is in the .service file)

[Unit]
Description=Run auto-shutdown check every minute

[Timer]
OnBootSec=2min
OnUnitActiveSec=1min
AccuracySec=30s
Persistent=true

[Install]
WantedBy=timers.target
